<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ceban">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ceban"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.main.MainActivity2"
            android:exported="true"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.Ceban.NoActionBar" />
        <activity
            android:name=".ui.assignment.detail.guru.studentanswer.StudentAnswerActivity"
            android:exported="true" />
        <activity
            android:name=".ui.assignment.detail.siswa.submission.edit.SubmissionEditActivity"
            android:exported="true" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.assignment.detail.siswa.submission.home.SubmissionActivity" />
        <activity android:name=".ui.assignment.detail.AssignmentDetailActivity" />
        <activity android:name=".ui.assignment.home.AssignmentActivity" />
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>